first create a instance as jenkins-server to connect jenkins as CI/CD pipeline to automatic the Build and test 
connect to server :
                    install java 
                    install the jenkins from the jenkins.io -> download -> redhate
                    start the jenkins in command line 
                    enable the jenkins
                    copy the public key and paste in chrome with :8080 port number
second create one job in jenkins to 
